# Анализ проблем и рисков архитектуры InsureTech

## 1. Текущее состояние (AS-IS)

### 1.1 Характеристики текущего решения
- Количество страховых компаний: 5 (планируется +5)
- Частота синхронизации core-app: каждые 15 минут
- Частота синхронизации ins-comp-settlement: 1 раз в сутки (ночью)
- Тип взаимодействия - cинхронный REST API  
- Хранение данных - локальные реплики в каждом сервисе

## 2. Выявленные проблемы

### 2.1 Проблема синхронного взаимодействия

**Описание:** При каждом запросе к `ins-product-aggregator` сервис синхронно опрашивает все 5 страховых компаний, агрегирует данные и возвращает результат.

**Последствия:**
- Время ответа определяется самой медленной страховой компанией
- При недоступности одной СК весь запрос может завершиться ошибкой
- Высокая связанность (coupling) между сервисами

**Риск при росте:** При увеличении до 10 СК:
- Время ответа увеличится пропорционально
- Вероятность ошибки возрастёт
- Нагрузка на ins-product-aggregator вырастет в 2 раза

### 2.2 Проблема устаревания данных

**Описание:** Локальные реплики в `core-app` обновляются каждые 15 минут, в `ins-comp-settlement` - раз в сутки.

**Последствия:**
- Данные о продуктах могут быть неактуальным до 15 минут
- Клиенты могут видеть устаревшие тарифы
- Возможны ошибки при оформлении страховки по старым условиям

**Риск при росте:** 
- При увеличении количества СК объём данных для синхронизации вырастет
- Увеличится время синхронизации
- Возрастёт нагрузка на сеть и БД

### 2.3 Проблема отказоустойчивости

**Описание:** Ошибки взаимодействия с API страховых компаний приводят к сбоям синхронизации.

**Последствия:**
- Локальные реплики не обновляются
- Сервисы работают с устаревшими данными
- Нет механизма гарантированной доставки обновлений

**Риск при росте:**
- С 10 СК вероятность сбоя при синхронизации возрастёт
- Увеличится количество инцидентов
- Потребуется больше ресурсов на мониторинг и реагирование

### 2.4 Проблема масштабируемости

**Описание:** Синхронный polling-подход плохо масштабируется.

**Последствия:**
- При увеличении частоты опроса растёт нагрузка на ins-product-aggregator
- Невозможно получать обновления в реальном времени
- Дублирование логики хранения данных в нескольких сервисах

**Риск при росте:**
- При 10 СК и сохранении интервала 15 минут - 40 запросов к внешним API в час
- При необходимости уменьшить интервал - кратный рост нагрузки

### 2.5 Проблема консистентности данных

**Описание:** Разные сервисы имеют разные версии данных о продуктах.

**Последствия:**
- `core-app` и `ins-comp-settlement` могут иметь разные данные
- Возможны расхождения при формировании реестров
- Сложность отладки и диагностики проблем

**Риск при росте:**
- С увеличением объёма данных расхождения станут более частыми
- Усложнится reconciliation процесс

### 2.6 Проблема batch-запроса оформленных страховок

**Описание:** `ins-comp-settlement` раз в сутки запрашивает все оформленные страховки из `core-app`.

**Последствия:**
- Большой объём данных передаётся за один запрос
- Высокая нагрузка на `core-app` в момент запроса
- При ошибке - потеря всех данных за день

**Риск при росте:**
- С ростом количества оформлений объём данных увеличится
- Возрастёт время выполнения запроса
- Увеличится риск timeout'ов

## 3. Матрица рисков

| Риск | Вероятность | Влияние | Приоритет |
|------|-------------|---------|-----------|
| Увеличение времени ответа при 10 СК | Высокая | Высокое | 🔴 Критический |
| Сбой синхронизации из-за недоступности СК | Высокая | Среднее | 🟠 Высокий |
| Несогласованность данных между сервисами | Средняя | Высокое | 🟠 Высокий |
| Timeout при batch-запросе страховок | Средняя | Высокое | 🟠 Высокий |
| Потеря обновлений продуктов | Средняя | Среднее | 🟡 Средний |
| Перегрузка ins-product-aggregator | Средняя | Среднее | 🟡 Средний |

## 4. Рекомендации

### 4.1 Переход на Event-Driven архитектуру

**Обоснование:**
- Асинхронное взаимодействие устраняет зависимость от времени ответа СК
- Гарантированная доставка событий через брокер сообщений
- Возможность обработки событий в реальном времени
- Естественное масштабирование через партиционирование

### 4.2 Внедрение паттерна Transactional Outbox

**Обоснование:**
- Гарантирует атомарность записи в БД и публикации события
- Устраняет проблему "dual write"
- Обеспечивает exactly-once семантику доставки
- Критически важен для событий об оформленных страховках

### 4.3 Использование Apache Kafka как брокера сообщений

**Обоснование:**
- Высокая пропускная способность
- Гарантированная доставка и порядок сообщений в партиции
- Возможность replay событий
- Retention политики для хранения истории

## 5. Заключение

Текущая архитектура с синхронным REST-взаимодействием и polling-подходом не готова к масштабированию до 10 страховых компаний. Основные риски связаны с:

1. **Производительностью** - синхронные запросы к 10 СК критически увеличат latency
2. **Надёжностью** - отсутствие гарантированной доставки обновлений
3. **Консистентностью** - разные версии данных в разных сервисах

Переход на Event-Driven архитектуру с использованием Kafka и паттерна Transactional Outbox позволит:
- Устранить синхронную зависимость между сервисами
- Обеспечить гарантированную доставку событий
- Получать обновления в near real-time
- Масштабироваться без изменения архитектуры
